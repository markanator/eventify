# The app imports "cropperjs/dist/cropper.css" directly, but cropperjs is only a
# transitive dependency (via react-cropper). npm's flat node_modules hoisted it;
# pnpm's default symlinked layout does not, which breaks `vite build`.
# Use a hoisted node_modules layout to preserve the previous resolution behaviour.
node-linker=hoisted
